version: "3.9"

services:
  backend:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: ex-batch-addin   
    ports:
      - "8000:8000"
    restart: unless-stopped
    volumes:
      - ./storage:/app/storage

