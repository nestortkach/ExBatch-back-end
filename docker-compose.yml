version: "3.9"

services:
  backend:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: exbatch-backend
    ports:
      - "8000:8000"
    volumes:
      - .:/app
    environment:

      PORT: 8000
    restart: unless-stopped

